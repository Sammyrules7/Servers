apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: main-rw-postgres-pooler
  namespace: postgres
spec:
  cluster:
    name: postgres
  instances: 2
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "1000"
      default_pool_size: "20"
