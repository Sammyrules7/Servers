apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: attic
  namespace: atticd
spec:
  parentRefs:
    - name: gateway
      namespace: ingress
  hostnames:
    - "attic.maio-tech.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: atticd-http
          port: 80
          #timeouts:
          #  request: 15m
          #  backendRequest: 15m
---
apiVersion: v1
kind: Service
metadata:
  name: atticd-http
  namespace: atticd
spec:
  selector:
    app: attic
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
