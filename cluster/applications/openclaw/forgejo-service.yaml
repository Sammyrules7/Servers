apiVersion: v1
kind: Service
metadata:
  name: forgejo-http
  namespace: openclaw
spec:
  type: ExternalName
  externalName: forgejo-http.forgejo.svc.cluster.local
  ports:
    - name: http
      port: 3000
      targetPort: 3000
